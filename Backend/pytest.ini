[pytest]
# Backend/ on sys.path so bare `pytest` resolves `from tables import ...`.
pythonpath = .
testpaths = Tests
# A deadlocked Postgres test fails instead of hanging the run.
timeout = 120
timeout_method = thread
