# Where Next forwards /api/* (server side). Defaults to http://127.0.0.1:8000 when unset.
API_ORIGIN=
# 1 shows the demo-only UI: demo login box, demo forgot-password text, "Reset demo data".
NEXT_PUBLIC_DEMO_MODE=
